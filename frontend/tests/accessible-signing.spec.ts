import { expect, test } from '@playwright/test';
const data = { signer: { id: 1, name: 'Nguyễn Văn A', email: 'a@example.test', role: 'signer', status: 'pending' }, sign_request: { id: 2, title: 'Hợp đồng', message: '', deadline: '', created_at: '' }, document: { id: 3, title: 'Hợp đồng', original_file_name: 'hop-dong.pdf', created_at: '' }, fields: [{ id: 11, type: 'signature', pageIndex: 0, xPct: 20, yPct: 20, widthPct: 20, heightPct: 10, label: 'Chữ ký' }] };
test('UX-003 keyboard user can open typed-signature dialog after OTP verification', async ({ page }) => { await page.route('http://127.0.0.1:4010/**', route => { const url = route.request().url(); if (url.includes('/verify-otp')) return route.fulfill({ contentType: 'application/json', body: JSON.stringify({ data: {} }) }); if (url.includes('/public/sign/')) return route.fulfill({ contentType: 'application/json', body: JSON.stringify({ data }) }); return route.fulfill({ contentType: 'application/json', body: JSON.stringify({}) }); }); await page.goto('/sign/token'); await page.getByLabel('Mã OTP').fill('123456'); await page.getByRole('button', { name: /Xác thực OTP/ }).click(); await page.getByRole('button', { name: /Ký một lần/ }).press('Enter'); await expect(page.getByRole('dialog')).toBeVisible(); await page.getByRole('tab', { name: 'Gõ tên' }).press('Enter'); await page.getByLabel('Nhập tên của bạn').fill('Nguyễn Văn A'); await page.screenshot({ path: '../docs/ux/evidence/ux003-typed-signature-desktop.png', fullPage: true }); await page.keyboard.press('Escape'); await expect(page.getByRole('dialog')).toHaveCount(0); await page.setViewportSize({ width: 375, height: 812 }); await page.screenshot({ path: '../docs/ux/evidence/ux003-typed-signature-mobile.png', fullPage: true }); });
